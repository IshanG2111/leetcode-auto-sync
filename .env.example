# GEMINI_API_KEY: Required for Google Gemini API integration (analysis & metadata extraction).
# Generate your key from the Google AI Developer Portal.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# APP_URL: The base URL where this application runs.
# Used for absolute URL generation, OAuth callbacks, and routing.
APP_URL="http://localhost:3000"
